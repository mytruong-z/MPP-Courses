1.
Runnable r = () -> {
    int[][] products = new int[s][t];
    for (int i = 0; i < s; i++) {
        for(int j = i + 1; j < t; j++) {
            products[i][j] = i * j;
        }
    }
};
** Answer: **
a) Parameters: None. This lambda expression doesn't have any parameters.
b) Free Variables:
    - s: A free variable used in the declaration of the 2D array.
    - t: Another free variable used in the declaration of the 2D array, as well as in the loop conditions.
** End Answer: **

2.
Comparator<String> comp = (s, t) -> {
    if (ignoreCase == true) {
        return s.compareToIgnoreCase(t);
    } else {
        return s.compareTo(t);
    }
};
** Answer: **
a) Parameters:
    - s: A String representing the first operand for comparison.
    - t: A String representing the second operand for comparison.
b) Free Variables:
- ignoreCase: A free variable used in the conditional statement. It is assumed to be a variable declared outside the lambda expression.
