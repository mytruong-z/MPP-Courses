 Does it make sense to use inheritance here? Explain.
 Base on the class diagram, it does not make sense to use inheritance here.
 Because if a cylinder inherits from a circle, it can be used to calculate the area.
 Otherwise it has no meaning.